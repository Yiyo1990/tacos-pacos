import { FormatoMonedaDirective } from './formato-moneda.directive';

describe('FormatoMonedaDirective', () => {
  it('should create an instance', () => {
    /*const directive = new FormatoMonedaDirective();
    expect(directive).toBeTruthy();*/
  });
});
