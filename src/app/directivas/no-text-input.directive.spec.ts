import { NoTextInputDirective } from './no-text-input.directive';

describe('NoTextInputDirective', () => {
  it('should create an instance', () => {
    /*const directive = new NoTextInputDirective();
    expect(directive).toBeTruthy();*/
  });
});
